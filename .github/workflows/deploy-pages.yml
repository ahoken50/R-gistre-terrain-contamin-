      - name: Install dependencies
        run: npm ci

      - name: Build               # ➜ nouvelle étape
        run: npm run build        #   lance Vite (dist/)

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
-         path: './public'
+         path: './dist'          # ➜ publier le build Vite
